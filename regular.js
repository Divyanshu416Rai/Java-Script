const pattern = /e/; // Regular expression to match the letter 'e'
let x = pattern.test("the best things in life are best"); // Test if 'e' is in the string
console.log(x); // Output the result

cars=[]
for(let i=0;cars.length;i++){
 text.t=cars[i]+"<br>";
}

const person= {fname:"", lname:"",age:""}
let text="";
for(let x in person)
{
    textt=person[x];
}